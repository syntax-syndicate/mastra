# Base URL of the Mastra API server. Leave unset for local development: Vite
# proxies `/support/*` to http://localhost:4111 (see vite.config.ts). Set it
# only when the UI and Mastra app run at separate addresses.
VITE_API_BASE_URL=
