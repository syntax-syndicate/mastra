import { countdown } from './countdown';
import { documentBatch } from './document-batch';
import { requestReview } from './request-review';

export const previewWorkflows = { requestReview, documentBatch, countdown };
