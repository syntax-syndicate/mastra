---
'@mastra/voice-openai': patch
---

Fixed install warnings when `@mastra/voice-openai` is used alongside Zod 4 by updating the OpenAI SDK to v6. Existing speech and transcription behavior is unchanged.
