---
'@mastra/core': patch
---

Channel approval cards (Slack, Telegram, etc.) now show the sub-agent tool awaiting approval and its arguments, instead of the supervisor's `agent-<name>` delegation call.
