---
'@mastra/server': patch
---

Added \`orderBy\` query parameter to the dataset, dataset items, experiments and experiment results list routes (for example \`GET /api/datasets?orderBy[field]=name&orderBy[direction]=ASC\`).
